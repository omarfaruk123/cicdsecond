name: Implement Cicd of laravel project

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  apply_cicd_process:
    runs-on: ubuntu-latest
    steps:
      - name: Need checkout
        uses: actions/checkout@v3
